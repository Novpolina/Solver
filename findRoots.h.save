#ifndef FINDROOTS_H_INCLUDED
#define FINDROOTS_H_INCLUDED


// Created by avtol on 24.08.2023.
//

const float EPSILON = 0.000001;
const float DEFOLT = 0; // TODO: spelling

enum comparation{
    EQUVAL, FIRST, SECOND
};

enum numberOfRoots{
    NO_ROOTS = 0, ONE_ROOT, TWO_ROOTS, INFIN_ROOTS // TODO: enum explicit numbers
};

struct coef{
    float a;
    float b;
    float c;
};

struct roots{
    int num;
    float x1;
    float x2;
    float comp;
};

roots makeRoots(int, float, float, float);

coef makeCoef(float, float, float);

//
roots findRoots(struct coef);

int compare(float, float); // TODO: Why is it here?

roots findRoots(coef);

#endif // FINDROOTS_H_INCLUDED
